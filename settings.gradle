pluginManagement {
    repositories {
        google()
        maven {
            url "https://repo1.maven.org/maven2/"
            content {
                includeGroup "io.netty"
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven {
            url "https://repo1.maven.org/maven2/"
            content {
                includeGroup "io.netty"
            }
        }
        mavenCentral()
    }
}
rootProject.name = "AstralTyper"
include ':app'
