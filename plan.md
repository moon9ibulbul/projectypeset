Ah! Wait! Look at line 1816!
```kotlin
    canvas.translate(dx, dy)
    drawContent(canvas, layout, w, h, skipEffects = false)
```
This is the default drawing branch (when `useHardwareTransformEffects` is false, or when `!isWarpActive` and `!isPerspectiveActive`).
If `hasTransform` is FALSE, then `isDrawingStrokePass` is FALSE.
But what if `hasTransform` is TRUE, and we call `drawContent` from inside `getAssembledLetterWarpBitmapCached` or similar?
Wait, if `hasTransform` is TRUE, we evaluate `isDrawingStrokePass = !isRoughStroke && shadowRadius <= 0f && shadowThickness <= 0f`.
So `isDrawingStrokePass` is evaluated ONCE in `drawBase()` at the very top.
If `hasTransform` is true (e.g. Warp is active), `isDrawingStrokePass` STAYS TRUE during the entire `drawBase()` execution!
Wait! If `isDrawingStrokePass` stays true, then when it actually draws the FILL, `isDrawingStrokePass` is still true!
Wait, no! If `hasTransform` is true, the drawing is done OFF-SCREEN into `cleanContentCache`!
Let's see: `cleanContentCache` is generated by `drawCleanContent()`.
Does `drawCleanContent()` call `drawContent()`? YES.
Wait! During `drawCleanContent()`, what is the value of `isDrawingStrokePass`?
It is whatever it was set to in `drawBase()`!
If `isDrawingStrokePass` is true, then `drawCleanContent()` will draw into `cleanContentCache` with `suppressBg = true`!
This means the `cleanContentCache` (which is the actual visual representation of the layer) will NOT HAVE THE HIGHLIGHT COLOR AT ALL!
Oh! `isDrawingStrokePass` was introduced for the hardware-accelerated warp rendering, but it leaked into the software rendering!
Wait, in `TextLayer.kt`:
```kotlin
                    val hasStrokes = !isDrawingClippingMask && strokeWidthToUse > 0f && !isRoughStroke && isDrawingStrokePass
```
If `hasStrokes` is true, it extracts alpha from `morphedBmp` (which is drawn from `cleanContentCache`).
But if `cleanContentCache` has NO background color because `isDrawingStrokePass == true`, then the strokes won't have it (good), but the FILL ALSO WON'T HAVE IT!
Wait... but after generating the strokes, it calls `drawWarped` or similar to draw the fill, and uses `morphedBmp`!
`morphedBmp` is created from `cleanContentCache`!
So if `cleanContentCache` is generated WITHOUT the background color, the background color is LOST forever!

Wait! Let's check `drawMain` in `TextLayer.kt`:
```kotlin
            if (silhouetteColor != null) {
                ...
                drawLayoutSafe(targetCanvas, isDrawingStrokePass || isDrawingShadowPass)
            } else if (isDrawingShadowPass) {
                ...
                drawLayoutSafe(targetCanvas, true)
            } else {
                val drawFillContent = { fillCanvas: Canvas ->
                    ...
                        drawLayoutSafe(fillCanvas, isDrawingStrokePass || isDrawingShadowPass)
                    ...
```
Ah! I specifically added `isDrawingStrokePass || isDrawingShadowPass` as the `suppressBg` argument for the FILL pass!
I thought that `isDrawingStrokePass` was ONLY true when generating a mask.
But `isDrawingStrokePass` is actually TRUE for the entire `drawBase()` if there is a transform!
Wait, if it's true for the entire `drawBase()`, then the FILL will ALWAYS be drawn WITHOUT the highlight when Warp is active!

Let's check if `isDrawingStrokePass` is true when there are no transforms:
If `hasTransform` is FALSE, `isDrawingStrokePass` is FALSE (its default value).
If `hasTransform` is FALSE, `isDrawingStrokePass` is FALSE, so `suppressBg` is FALSE in the Fill pass!
Wait... so if `hasTransform` is FALSE, the highlight SHOULD appear!
Did the user say the highlight color doesn't appear AT ALL? Or only when warped?
"Review : Highlight color tidak muncul di canvas" (Highlight color does not appear on canvas).
Wait, if I have `hasTransform == false`, does it appear?
Wait... in `drawLayoutSafe`:
```kotlin
        fun drawLayoutSafe(canvas: android.graphics.Canvas, suppressBg: Boolean) {
            val spannable = layout.text as? android.text.Spannable
            val bgSpans = spannable?.getSpans(0, spannable.length, android.text.style.BackgroundColorSpan::class.java)

            if (suppressBg && bgSpans != null && bgSpans.isNotEmpty()) {
```
If `suppressBg` is FALSE, it just calls `layout.draw(canvas)`. It does NOT touch `bgSpans`.
So if `hasTransform == false`, it SHOULD appear!
BUT wait! When I replaced the `layout.draw(canvas)` in the stroke blocks:
```kotlin
            // 2. Stroke
            if (!isDrawingClippingMask && !isDrawingStrokePass && strokeWidthToUse > 0f) {
                paint.style = Paint.Style.STROKE
                ...
                drawLayoutSafe(targetCanvas, true) // Always suppress background for stroke!
            }
```
If we are drawing the stroke, `suppressBg` is `true`.
So we remove `BackgroundColorSpan`, draw layout (which draws stroke text), and then add them back.
Then it goes to:
```kotlin
            // 3. Fill
            paint.style = Paint.Style.FILL
            paint.strokeWidth = 0f
            paint.pathEffect = null
            ...
                drawLayoutSafe(fillCanvas, isDrawingStrokePass || isDrawingShadowPass)
```
If `hasTransform == false`, then `isDrawingStrokePass` is `false`. So `suppressBg` is `false`.
So it should just call `layout.draw(canvas)` with the spans intact!
Why would the highlight color NOT appear AT ALL?
Let's check the `removeSpan` and `setSpan` logic in `drawLayoutSafe`:
```kotlin
                val spanData = bgSpans.map { it to Pair(spannable.getSpanStart(it), spannable.getSpanEnd(it)) }
                bgSpans.forEach { spannable.removeSpan(it) }
                layout.draw(canvas)
                spanData.forEach { spannable.setSpan(it.first, it.second.first, it.second.second, android.text.Spannable.SPAN_EXCLUSIVE_EXCLUSIVE) }
```
Wait! `bgSpans.forEach { spannable.removeSpan(it) }` mutates the text.
But `StaticLayout` caches some things! Wait, does `StaticLayout` cache the text rendering?
No, `StaticLayout.draw` iterates through spans.
BUT wait! What if `spannable` is NOT mutated correctly, or `setSpan` invalidates something that breaks `StaticLayout`?
Actually, `BackgroundColorSpan` DOES NOT affect metrics. So adding/removing it doesn't break `StaticLayout` metrics.
BUT wait! We are operating on `layout.text`.
`layout.text` in `cachedLayout` is `SpannableStringBuilder`.
If we call `removeSpan` and `setSpan`, does it trigger a text watcher or something? No.

Wait. Is it possible that `bgSpans` doesn't find `BackgroundColorSpan` because we used the wrong class? No, `android.text.style.BackgroundColorSpan::class.java`.
Is it possible that `isDrawingStrokePass` is ALWAYS true?
No, it's initialized to `false`.

Let's test if `layout.text` is actually mutable!
Wait, `layout.text` is the `tempText` we created in `ensureLayout()`.
```kotlin
        val tempText = SpannableStringBuilder(text)
        ...
        cachedLayout = StaticLayout(...)
```
Yes, it is mutable.

Wait! In `drawMain`:
```kotlin
            if (!isDrawingClippingMask && !isDrawingStrokePass && strokeWidthToUse > 0f) {
                // ...
                drawLayoutSafe(targetCanvas, true)
            }
```
In `drawLayoutSafe(targetCanvas, true)`, we REMOVE the spans, draw the layout (Stroke), and ADD the spans back.
BUT WAIT!
If we add the spans back using `Spannable.SPAN_EXCLUSIVE_EXCLUSIVE`, does it matter? The original span might have had different flags!
Wait... `bgSpans` is an array of `BackgroundColorSpan`.
When we remove it and add it back, we don't restore the original span flags! (Though it shouldn't matter much for drawing).

Wait, the user says "Highlight color tidak muncul di canvas".
Let's look at `EditorActivity.kt` and how highlight colors are applied.
```kotlin
                    val currentHighlightColor = getActiveHighlightColor(layer)
                    ColorPickerHelper.showColorPickerDialog(this@EditorActivity, currentHighlightColor) { color ->
                        val et = activeEditText
                        if (et != null) {
                            applySpanToSelection(android.text.style.BackgroundColorSpan(color))
                        }
```
`applySpanToSelection` adds the span to the `EditText`'s text, which then syncs to the layer!
```kotlin
    private fun applySpanToSelection(span: Any) {
        val et = activeEditText ?: return
        val start = et.selectionStart
        val end = et.selectionEnd
        if (start != -1 && end != -1 && start != end) {
            et.text.setSpan(span, start, end, Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
        } else {
            // If no selection, apply to whole text?
            et.text.setSpan(span, 0, et.text.length, Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
        }
        val layer = activeLayer as? TextLayer
        if (layer != null) {
            layer.text = SpannableStringBuilder(et.text)
            canvasView.invalidate()
        }
    }
```
When `layer.text` is updated, `ensureLayout()` is called, which creates a `SpannableStringBuilder(text)`.
Wait! In `SpannableStringBuilder(text)`, the spans are copied.
If `drawLayoutSafe` removes the span from `layout.text`, it removes it from `cachedLayout.text`.
But `cachedLayout.text` is exactly the `tempText` we created!
Wait, if it removes it and adds it back, it should be fine.

Let's rethink. If `suppressBg = isDrawingStrokePass || isDrawingShadowPass` in the Fill pass causes the highlight color to disappear, maybe `isDrawingStrokePass` IS always true?
Let's check `isDrawingStrokePass` again.
```kotlin
        if (hasTransform) {
            isDrawingStrokePass = !isRoughStroke && shadowRadius <= 0f && shadowThickness <= 0f
        }
```
If `hasTransform` is `true`, it is `true`.
What if `hasTransform` is `false`? It remains `false`.
But wait! If `isDrawingStrokePass` is `true` for the entire `drawBase()` when `hasTransform` is `true`, then the Fill pass will have `suppressBg = true`!
This means if you apply Warp, the highlight color will disappear completely!
Is the user using Warp? We don't know, but if they are, that would explain it.

But wait! What if the user is NOT using Warp? Why would it disappear?
Let's look at `drawLayoutSafe` again:
```kotlin
        fun drawLayoutSafe(canvas: android.graphics.Canvas, suppressBg: Boolean) {
            val spannable = layout.text as? android.text.Spannable
            val bgSpans = spannable?.getSpans(0, spannable.length, android.text.style.BackgroundColorSpan::class.java)

            if (suppressBg && bgSpans != null && bgSpans.isNotEmpty()) {
                val spanData = bgSpans.map { it to Pair(spannable.getSpanStart(it), spannable.getSpanEnd(it)) }
                bgSpans.forEach { spannable.removeSpan(it) }
                layout.draw(canvas)
                spanData.forEach { spannable.setSpan(it.first, it.second.first, it.second.second, android.text.Spannable.SPAN_EXCLUSIVE_EXCLUSIVE) }
            } else {
                layout.draw(canvas)
            }
        }
```
If `suppressBg` is `false`, it doesn't remove anything, it just calls `layout.draw(canvas)`.
Wait! Is `bgSpans` empty? No, we check `bgSpans.isNotEmpty()`.
If `suppressBg` is `true`, it removes it, draws, and restores it.
Wait! What if restoring it fails?
When we do `spanData.forEach { spannable.setSpan(...) }`, we use the same `it.first` (the span object).
Does `StaticLayout` cache the fact that there are no background spans during `layout.draw()`?
Ah!!!
When `layout.draw(canvas)` is called, Android's `TextLine` fetches the spans.
If we remove the spans, `layout.draw(canvas)` executes without them.
Then we add them back. Next time `layout.draw(canvas)` is called, they are there.

Wait... look at `drawMain`!
```kotlin
            // 2. Stroke
            if (!isDrawingClippingMask && !isDrawingStrokePass && strokeWidthToUse > 0f) {
                paint.style = Paint.Style.STROKE
                paint.strokeWidth = strokeWidthToUse
                ...
                drawLayoutSafe(targetCanvas, true)
                drawTailPath(targetCanvas, paint)
            }

            // 3. Fill
            paint.style = Paint.Style.FILL
            ...
                drawLayoutSafe(fillCanvas, isDrawingStrokePass || isDrawingShadowPass)
```
If we run `// 2. Stroke`, `suppressBg` is `true`. We remove spans, draw, and ADD THEM BACK.
Then we run `// 3. Fill`. `suppressBg` is `false`. We just call `layout.draw(fillCanvas)`. The spans SHOULD be there because we added them back!
Why would they not appear?

Wait! In `drawMain`:
```kotlin
        val drawMain = { targetCanvas: Canvas ->
```
Does `drawMain` get called multiple times? Yes.
Is it possible that `spanData` mapping doesn't work correctly?
```kotlin
val spanData = bgSpans.map { it to Pair(spannable.getSpanStart(it), spannable.getSpanEnd(it)) }
```
`spannable.getSpanStart(it)` returns the start index.
But wait! `spanData.forEach { spannable.setSpan(it.first, it.second.first, it.second.second, android.text.Spannable.SPAN_EXCLUSIVE_EXCLUSIVE) }`
Is it possible that `BackgroundColorSpan` objects get corrupted or ignored by `StaticLayout` once removed and re-added?
No.

Wait, is `layout.text` definitely the same `Spannable`? Yes, `layout.text` is the `CharSequence` we passed to `StaticLayout`.
BUT WAIT. When `StaticLayout` is created, if the text is NOT a `Spanned`, it might wrap it? No, `tempText` is `SpannableStringBuilder`.
Let's check if `isDrawingStrokePass` is true all the time.
What if `isDrawingStrokePass` is actually ALWAYS `true` because `hasTransform` is ALWAYS `true`?
```kotlin
        val hasTransform = scaleX != 1f || scaleY != 1f || rotation != 0f || isWarpActive || isPerspectiveActive
```
Wait! `scaleX != 1f || scaleY != 1f || rotation != 0f`!!!
If a user scales or rotates the text layer using the bounding box controls, `hasTransform` becomes TRUE!
And if they have NO shadow and NO rough stroke, `isDrawingStrokePass = true`!
YES! `hasTransform` is almost ALWAYS true when a user interacts with a layer (scaling/rotating it)!
And if `isDrawingStrokePass` is `true`, then `suppressBg = isDrawingStrokePass || isDrawingShadowPass` evaluates to TRUE for the FILL pass!
So the background is SUPPRESSED for the FILL pass just because the layer is scaled or rotated!
That is exactly why the highlight color doesn't appear!
Because `isDrawingStrokePass` is used to decide `suppressBg` in the Fill pass!

Wait! Why did I add `isDrawingStrokePass` to `suppressBg` for the Fill pass?
Because I thought: "If `isDrawingStrokePass` is true, we are drawing the fill to generate the stroke mask via `extractAlpha`. We shouldn't include the highlight box in the stroke mask."
This logic IS correct! We shouldn't include the highlight box in the stroke mask.
BUT, if `isDrawingStrokePass` is true, does the regular Fill pass EVER get drawn?
Let's look at `drawBase()` where the `hasTransform` and `extractAlpha` logic is:

```kotlin
        try {
            if (useHardwareTransformEffects) {
                val drawTransformed = { targetCanvas: Canvas ->
                    if (isWarpActive) {
                       ...
                    } else if (isPerspectiveActive) {
                       ...
                    } else {
                        // Standard matrix transform
                        val saveCount = targetCanvas.save()
                        val cx = w / 2f
                        val cy = h / 2f
                        targetCanvas.translate(cx, cy)
                        targetCanvas.scale(scaleX, scaleY)
                        targetCanvas.rotate(rotation)
                        targetCanvas.translate(-cx, -cy)

                        val hasStrokes = !isDrawingClippingMask && strokeWidthToUse > 0f && !isRoughStroke && isDrawingStrokePass
                        if (hasStrokes) {
                            // GENERATE STROKES using extractAlpha on morphedBmp (which is cleanContentCache)
                            // ...
                        }

                        // THEN it draws the shadow ...

                        // THEN it draws the content!
                        if (activeErasePath != null) { ... }
                        else {
                            targetCanvas.drawBitmap(cleanContentCache!!, null, destRect, paint)
                        }
                        targetCanvas.restore()
                    }
```
Wait! The actual drawing to the screen is `targetCanvas.drawBitmap(cleanContentCache!!, null, destRect, paint)`.
`cleanContentCache` is generated by `drawCleanContent()`.
Does `drawCleanContent()` call `drawContent()`? Yes.
Does `drawContent()` call `drawMain()`? Yes.
When `drawCleanContent()` runs, what is the value of `isDrawingStrokePass`?
It is TRUE! (because it was set at the top of `drawBase`).
If it is TRUE, then `drawMain` will generate `cleanContentCache` WITH `suppressBg = true`!
This means `cleanContentCache` (which is used for BOTH the extractAlpha stroke AND the final fill) will NOT have the highlight color!
Since `cleanContentCache` is used for the final fill, the final fill is missing the highlight color!

Ah!!! `cleanContentCache` is used as the base for EVERYTHING in the hardware path!
If we suppress the highlight color in `cleanContentCache`, we lose it forever!
But if we DON'T suppress the highlight color in `cleanContentCache`, then the extractAlpha stroke WILL stroke the highlight box!

How do we solve this?
The standard native stroke (`paint.style = STROKE`) works perfectly: it strokes the text, and we can suppress the background when drawing the STROKE pass, and allow it in the FILL pass.
But for `extractAlpha` strokes, the stroke is generated from the alpha channel of `morphedBmp` (which is `cleanContentCache`).
If `cleanContentCache` has the background highlight, the highlight becomes part of the alpha channel, so the blur mask includes the highlight!
To fix this, we would need TWO separate bitmaps: one for the stroke mask (text without highlight) and one for the fill (text with highlight)!
But `TextLayer` caches `cleanContentCache` heavily for performance. Generating two software bitmaps for every frame would be slow.

Is there a way to generate the stroke natively even when transformed?
Wait! In `drawMain`, the native strokes are:
```kotlin
            // 0. Triple Stroke
            if (!isDrawingClippingMask && !isDrawingStrokePass && tripleStrokeWidthToUse > 0f && doubleStrokeWidthToUse > 0f && strokeWidthToUse > 0f) {
                // native stroke
            }
```
They are disabled when `isDrawingStrokePass == true`.
Why was `isDrawingStrokePass` added?
Let's look at the memory:
> "To prevent visual bloat and excessive thickness of strokes on warped layers, both `TextLayer.kt` and `ShapeLayer.kt` dynamically scale standard, double, and triple stroke widths by 50% ... when a warp transformation is active (`isWarpActive` is true)."
> "To prevent the standard built-in shadow from being enveloped by the stroke during warp/perspective transformations in `TextLayer.kt` and `ShapeLayer.kt`, `isDrawingStrokePass` is explicitly evaluated to `false` when a shadow is active (e.g., `shadowRadius > 0f` or `shadowThickness > 0f`). This forces the native stroke to be drawn pre-warp, bypassing the post-warp `extractAlpha` stroke generation which would incorrectly outline the combined fill and shadow."

Wait! The memory says that the post-warp `extractAlpha` stroke generation is used specifically for Warp/Perspective, but if there's a shadow, it evaluates `isDrawingStrokePass = false` to fall back to the pre-warp native stroke!
If `isDrawingStrokePass = false`, it draws the native strokes (which we fixed by suppressing the background).

Wait! What if we just FALL BACK to native stroke if `BackgroundColorSpan` is active?
If the text has a highlight color, we can force `isDrawingStrokePass = false`!
Just like how it's forced to `false` if `shadowRadius > 0f`!
Let's check `isDrawingStrokePass` assignment:
```kotlin
        if (hasTransform) {
            isDrawingStrokePass = !isRoughStroke && shadowRadius <= 0f && shadowThickness <= 0f
        }
```
If we add `&& !hasHighlightColor` to this condition, it will fall back to native stroke pre-warp, meaning the highlight won't be outlined, and it will draw correctly!
Let's check if we can easily detect `hasHighlightColor`.
```kotlin
        val hasHighlightColor = layout?.text?.let { text ->
            val spannable = text as? android.text.Spannable
            spannable?.getSpans(0, text.length, android.text.style.BackgroundColorSpan::class.java)?.isNotEmpty() == true
        } ?: false
```
If we add that, it will use native stroke. Native stroke is drawn inside `drawMain`, where we ALREADY implemented `suppressBg = true`!
So the native stroke will NOT stroke the background!
And because it's native stroke, it's drawn directly to `cleanContentCache` (as stroke + fill). Then the whole `cleanContentCache` is warped.
Wait! If the native stroke is drawn pre-warp, does it look good when warped?
Yes, the memory says `isDrawingStrokePass` is used to prevent the standard built-in shadow from being enveloped by the stroke. It falls back to pre-warp native stroke for shadows. We can do the exact same thing for highlights!

Let's test this!
Wait, but if we do this, what about `isDrawingStrokePass || isDrawingShadowPass` inside the `drawMain` FILL passes?
Currently I have:
```kotlin
drawLayoutSafe(fillCanvas, isDrawingStrokePass || isDrawingShadowPass)
```
If `isDrawingStrokePass` is TRUE (e.g. no highlight, just warp), then `suppressBg` is TRUE in the FILL pass.
Wait! If `suppressBg` is TRUE in the FILL pass, it will SUPPRESS the background color for normal text too!
But wait, if `isDrawingStrokePass` is true, we just established there is NO highlight color! So `suppressBg` doesn't do anything because `bgSpans` is empty!
So it's completely safe!
But what if we don't change `isDrawingStrokePass` and just fix `suppressBg` in the Fill pass?
If we just change `suppressBg` in the Fill pass to FALSE:
```kotlin
drawLayoutSafe(fillCanvas, isDrawingShadowPass)
```
Then the FILL pass WILL draw the highlight color!
But wait, if the FILL pass draws the highlight color, then `cleanContentCache` WILL HAVE the highlight color!
If `cleanContentCache` has the highlight color, and `isDrawingStrokePass` is TRUE, then `extractAlpha` will extract the alpha of the highlight color, and the stroke WILL outline the highlight box!
So we MUST force `isDrawingStrokePass = false` if there is a highlight color!
